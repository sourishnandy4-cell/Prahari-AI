<?xml version="1.0" encoding="utf-8"?>
<!--
  PRAHARI AI — Network Security Config
  Allows cleartext HTTP traffic to local LAN backend (192.168.x.x, 10.x.x.x)
  while keeping all internet traffic secured via HTTPS.
-->
<network-security-config>
    <!-- Allow HTTP to private/LAN IP ranges for local FastAPI backend -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Class C private (192.168.x.x) -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <!-- Class A private (10.x.x.x) -->
        <domain includeSubdomains="true">10.0.0.0</domain>
        <!-- Localhost for dev/test -->
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>

    <!-- Default: require HTTPS for all other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
